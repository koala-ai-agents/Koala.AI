"""Setup file for kola plugins package."""

from setuptools import find_packages, setup

setup(
    name="kola-plugins",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
)
